# Canonical production environment. Direct dependencies are exact-pinned in
# the referenced lock so local runs and GitHub Actions resolve the same API
# surface. Use requirements-ci-lock.txt for tests and
# requirements-backfill-lock.txt for the lightweight realized-price job.
-r requirements-production-lock.txt

# PyTorch is intentionally not installed in normal production. Part 2a is an
# optional, fail-closed sleeve and records a NOT_RECOMMENDED summary when torch
# is absent. Install a separately reviewed CPU-only torch build to evaluate it.
