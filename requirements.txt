# GasPriceForecast — Python requirements
# =========================================
# Install: pip install -r requirements.txt
#
# API keys required (all free):
#   FRED_API_KEY   — https://fred.stlouisfed.org/docs/api/api_key.html
#   OILPRICEAPI_KEY — https://www.oilpriceapi.com  (live commodity snapshot)
#   EIA_API_KEY    — https://www.eia.gov/opendata/register.php

# ── Core data ──────────────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.10

# ── Machine learning ───────────────────────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0
hmmlearn>=0.3

# ── Data fetching ──────────────────────────────────────────────────────────────
yfinance>=0.2.36
fredapi>=0.5
requests>=2.31

# ── Storage & serialization ────────────────────────────────────────────────────
duckdb>=0.10
pyarrow>=14.0

# ── Visualization ──────────────────────────────────────────────────────────────
plotly>=5.18

# ── Deep learning (LSTM sleeve — gas_part2a) ───────────────────────────────────
# Uncomment to install PyTorch and enable the LSTM sleeve. Safe to install
# eagerly: unlike the reference project's original design, the activation
# gate is enforced IN CODE — gas_part2a_lstm_sleeve.py reads
# gas_part2b_summary.json every run and skips itself unless
# xgb_sleeve_recommended is true (fail-closed on any read error;
# override with GASPRICE_FORCE_LSTM=1). CPU-only build is sufficient.
# torch>=2.0
